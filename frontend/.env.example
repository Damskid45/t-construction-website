# Copy to .env and point this at your deployed backend URL (Render, etc.)
VITE_API_URL=http://localhost:5000

# Password to access /admin (change this to something only you know)
VITE_ADMIN_PASSWORD=change-me
